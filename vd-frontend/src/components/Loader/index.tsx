/**
 * @function LoadingComponent
 * @returns A stateless component to handle loading state
 * during fetching of data
 */

export default function LoadingComponent() {
  return (
    <div>loading</div>
  )
}
