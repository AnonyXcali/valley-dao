/**
 * @function SearchComponent
 * @returns stateless component that renders when there are
 * no results.
 */
export default function ZeroState() {
  return (
    <div>
      No Results Available
    </div>
  )
}
